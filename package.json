{
  "name": "gemini-cli-rewrite",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "cli": "npm run -w @gemini/cli start",
    "build": "npm run build -ws",
    "test": "npm run test -ws"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}